/*
import { GeoLocationsService } from '../../services/geo-locations';

describe('GeoLocationsService', () => {
	describe('.getAddressByCoordinatesUsingArcGIS(conditions)', () => {
		it('Finds location in Israel', async () => {
			const geoService = new GeoLocationsService();
		});
	});
});
*/
