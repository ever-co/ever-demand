import React from 'react';
import { Text, View } from 'react-native';

interface Props {}

const OrderDialog: React.FC<Props> = ({}) => (
	<View>
		<Text>OrderDialog</Text>
	</View>
);

export default OrderDialog;
