/**
 * How orders will be forwarded to the Store from Customers
 * (Phone, Email, etc)
 *
 * @enum {number}
 */
enum ForwardOrdersMethod {
	Unselected,
	Phone,
	Email,
}

export default ForwardOrdersMethod;
