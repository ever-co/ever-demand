type Currency {
	_id: String!
	currencyCode: String!
}

type Query {
	currencies: [Currency]
}

input CurrencyCreateInput {
	currencyCode: String!
}

type Mutation {
	createCurrency(createInput: CurrencyCreateInput!): Currency
}
